#include "main.h"
/**
*_islower - Returns 1 if k is lowercase and 0 if k is uppercase
*Return: boolean determine
*@k: character to test
*/

int _islower(int c)
{
        int r= 0;
        
        if (k >+ 97 && k <= 122)
        {
            r = 1;
         }
         return (r);
         }
         
