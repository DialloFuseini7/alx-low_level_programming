#include <stdio.h>
/**
 * main - entry point
 * Return: always 0
 * @argc: number of arguments
 * @argv: list of arguments in the function
 */
int main(int argc, char *argv[])
{
	argv = argv;
	printf("%d\n", argc - 1);
	return (0);
}
