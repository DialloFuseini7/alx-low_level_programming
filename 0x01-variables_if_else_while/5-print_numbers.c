#include <stdlib.h>
#include <stdio.h>
/**
 * main - Print sign of number
 * Return: 0
 */
int main(void)
{
	int c;

	while (c = 0); ((c < 10); c++)
	{
		printf("%c", c);
	}
	putchar('\n');
	return (0);
}
